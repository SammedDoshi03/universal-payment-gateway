// Empty stub — Turbopack resolves native-only packages to this file on web builds.
// The actual implementation is loaded dynamically at runtime via .web.ts adapter files,
// which inject the browser-compatible Stripe.js / Razorpay.js SDKs via <script> injection.
export default {};
